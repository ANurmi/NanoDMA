package:
  name: nanodma

dependencies:
  common_cells: { git: "https://github.com/pulp-platform/common_cells/tree/master", rev: master}

sources:
  files:
    - src/ndma_read_mgr.sv
    - src/ndma_write_mgr.sv
    - src/ndma.sv